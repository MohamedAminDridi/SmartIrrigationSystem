[build]
  command   = "npm run build"
  publish   = "dist"

# React Router: every path must serve index.html (client-side routing)
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
